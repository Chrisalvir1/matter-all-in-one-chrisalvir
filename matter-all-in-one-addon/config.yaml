name: "Matter All-in-One (Chrisalvir)"
description: "Universal Matter 1.4+ bridge for Home Assistant with native HomeKit HAP Secure Video (HKSV), live view streaming and enterprise discovery."
version: "1.4.80"
slug: "matter_all_in_one_bridge"
image: "ghcr.io/chrisalvir1/matter-all-in-one-chrisalvir"
init: false
host_network: true
ingress: true
ingress_port: 8283
arch:
  - amd64
  - aarch64
hassio_api: true
homeassistant_api: true
hassio_role: homeassistant
schema:
  host: str?
  token: str?
  group_by_device_id: bool?
  mdnsinterface: str?
  # Kept only so existing user configuration remains valid. Matter's required
  # LAN-only IPv6 link-local transport is no longer disabled by this option.
  ipv4_only: bool?
